{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/pocketbase"
  ],
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview"
}
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
