{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/pocketbase"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/pocketbase\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}